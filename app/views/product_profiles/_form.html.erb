<%= form_for(@product_profile) do |f| %>
  <% if @product_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_profile.errors.count, "error") %> prohibited this product_profile from being saved:</h2>

      <ul>
      <% @product_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
