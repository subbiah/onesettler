<%= form_for(@service_type_master) do |f| %>
  <% if @service_type_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_type_master.errors.count, "error") %> prohibited this service_type_master from being saved:</h2>

      <ul>
      <% @service_type_master.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
