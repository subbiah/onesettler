<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
		
		<%#= f.select_tag "category_id", options_from_collection_for_select(@category, "id", "categoryname")%>
    
    <select name="category">
    <% @category.each do |c| %>
    <option value="<%= c.id %>" selected="selected"><%= c.categoryname %></value>
    <%end%>
   </select>

  	</div>
  
  
  <div class="field">
    <%= f.label :section_id %><br>
    <%= f.number_field :section_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
