<%= form_for(@profile_question_master) do |f| %>
  <% if @profile_question_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_question_master.errors.count, "error") %> prohibited this profile_question_master from being saved:</h2>

      <ul>
      <% @profile_question_master.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
 <div class="field">
   	   Section <br>
   	   
 <form accept-charset="UTF-8" action="" id="section" method="get">  	   
  	<select name="section" >
    <% @section.each do |sec| %>
    <option value="<%= sec.id %>" selected="selected"><%= sec.section_name %></value>
    <%end%>
   </select>
   </form>
 </div>
 
  
  <div class="field">
    <%= f.label :questionname %><br>
    <%= f.text_field :questionname %>
  </div>
  <div class="field">
    <%= f.label :min_length %><br>
    <%= f.number_field :min_length %>
  </div>
  <div class="field">
    <%= f.label :max_length %><br>
    <%= f.number_field :max_length %>
  </div>
  <div class="field">
    <%= f.label :question_type %><br>
    <%= f.text_field :question_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
